#!/bin/bash

. $(dirname $0)/prelude.bash

# https://juicefs.com/docs/community/command_reference/#mount

exec sudo juicefs mount -d \
  --writeback \
  -o writeback_cache \
  --backup-skip-trash \
  --max-uploads=50 \
  sqlite3:///jfsmeta.db \
  /juicefs
